[tasks.build]
alias = "b"
run = "cargo build --release"

[tasks.copy-bin]
depends = ["build"]
run = "cp target/release/sync_highlights ~/bin"

[tasks.copy-man]
depends = ["build"]
run = "cp target/release/sync_highlights.1 ~/man/man1"

[tasks.install]
alias = "i"
depends = ["copy-bin", "copy-man"]
